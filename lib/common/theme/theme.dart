import 'package:app_dictionary/common/dictionary/dictionary.dart';

class CustomTheme {
  ThemeData get lightTheme {
    return ThemeData(primaryColor: Colors.amber);
  }

  ThemeData get darkTheme {
    return ThemeData(primaryColor: Colors.amber);
  }
}
